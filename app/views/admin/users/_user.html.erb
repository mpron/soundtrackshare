<div class="text-center post">
  <h2><%= link_to @username %></h2>
  <p>
    <% if can? :destroy, user %>
    <%= link_to "Delete Post", user, method: :delete, class: 'btn btn-danger', confirm: 'Are you sure you want to delete this user?' %>
    <% end %>
  </p>
</div>