<h1 class="text-center"><%= @username %> Shares</h1>
<div class="row">
  <div class="text-center formbox">
    <%= link_to "Back to the Home Page", root_path, class: "margin-space btn btn-block btn-default btn-lg" %>
  </div>
  <div class="text-center formbox">
    <h4>Community Submissions</h4>
  </div>
</div>
<div class="container text-center">
  <%= render @posts %>
</div>
<div class="text-center formbox">
  <%= link_to "Back to the Home Page", root_path, class: "margin-space-2 btn btn-block btn-default btn-lg" %>
  <%= will_paginate @posts %>
</div>
